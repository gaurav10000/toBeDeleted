// const num1 = 12412441
// // console.log(Number.isFinite(num1));
// console.log(num1.toLocaleString("en-IN"));

// one of the method to find a random number in a range
const max = 50
const min = 40
const randNumber = Math.floor(Math.random() * (max - min + 1)) + min
console.log(randNumber);