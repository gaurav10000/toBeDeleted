// arrays


const arr = [1,2,3,4,5,6]

// console.log(arr.push(7));
// arr.unshift([0,9])
// arr.shift()
// console.log(arr);

// console.log(typeof arr.join());


//  slice and splice

// console.log(arr.slice(1,3)); 
// console.log(arr); // we can conclude that while slicing it return a copy of og from defined starting point to defined second last point (i.e last element is not included) and og array does not change

// console.log(arr.splice(1,3));
// console.log(arr); // Removes elements from an array and, if necessary, inserts new elements in their place, returning the deleted elements. i.e cuts the defined section (including both staring and ending point) from og array.

